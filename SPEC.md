# task-manager 機能仕様書

## 1. 概要と非目標

### 概要

個人 1 人が PC の Web ブラウザで使うシンプルなタスク管理アプリ。データは利用ブラウザの localStorage に保存し、JSON エクスポート/インポートで手動移行できる。

### スコープ内（やること）

- タスクの追加・編集・完了トグル・削除
- 期限による自動グループ分け表示
- 重要フラグ（1 ビット）と任意のメモ
- JSON エクスポート/インポート
- OS のライト/ダーク設定への追従
- 主要操作のキーボードショートカット
- GitHub Pages へのデプロイ

### スコープ外（やらないこと）

将来欲しくなっても、このバージョンでは入れない。

- 複数ユーザー、ログイン、認証
- 複数デバイス間の自動同期、クラウド保存
- サブタスク、タスクの階層構造
- 繰り返しタスク（毎日 / 毎週など）
- リマインダ通知、デスクトップ通知
- 検索、フィルタ（「重要のみ表示」も含む）
- タグ、カテゴリ、プロジェクト分け
- マルチタブ間の自動同期
- ゴミ箱、削除アンドゥ
- 段階的な優先度（高 / 中 / 低）
- ドラッグ＆ドロップによる手動並べ替え
- テーマの手動切り替え

## 2. データモデル

### Task 型

```ts
type Task = {
  id: string;          // 一意 ID。crypto.randomUUID() で生成
  title: string;       // タイトル。必須、空文字列不可
  dueDate: string | null; // 期限。YYYY-MM-DD 形式、未設定なら null
  important: boolean;  // 重要フラグ
  memo: string;        // メモ。プレーンテキスト、未入力なら空文字列
  completed: boolean;  // 完了フラグ
  createdAt: string;   // 作成日時。ISO 8601（new Date().toISOString()）
  completedAt: string | null; // 完了した日時。未完了なら null
};
```

### 補足

- `dueDate` は **日付のみ** で時刻情報を持たない。タイムゾーンの混乱を避けるため、ローカルの「YYYY-MM-DD」文字列として扱う
- `id` は `crypto.randomUUID()`（モダンブラウザでサポート）を使う
- `createdAt` は並びの安定化と「グループ内の作成順ソート」用に保持
- `completedAt` は将来「最近完了したもの順に並べる」等に使える保険として持つが、今バージョンでは表示に使わない

## 3. 永続化

### localStorage の構造

- キー名: `task-manager:v1`
- 値: 以下の形の JSON 文字列

```ts
type StoredData = {
  schemaVersion: 1;        // 将来のマイグレーションのため
  tasks: Task[];
  lastCleanupDate: string; // 14 日自動クリアの最終実行日（YYYY-MM-DD）
};
```

### 読み込み

- アプリ起動時に 1 回読み込む
- パースに失敗した場合: 既存データは触らず、メモリ上は空配列で開始。ユーザーに「データを読み込めませんでした」とトーストで通知し、上書き保存を抑止して手動エクスポート/復旧の余地を残す
- `schemaVersion` が想定外の場合: 同上

### 書き込み

- タスクの変更があるたびに即書き込み（デバウンスなし）。件数が少ない前提なのでパフォーマンス問題は出ない
- `setItem` で例外（容量超過 `QuotaExceededError` 等）が出た場合: トースト通知のみ。アプリは落とさない

### JSON エクスポート

- ヘッダーのボタンクリックで `task-manager-YYYY-MM-DD.json` をダウンロード
- 内容は `StoredData` をそのまま整形した JSON

### JSON インポート

- ファイル選択 → 内容を `StoredData` としてバリデーション
  - `schemaVersion === 1` であること
  - `tasks` が配列であり、各要素に `id`, `title`, `completed` が存在し型が正しいこと
- バリデーション NG ならエラートーストを出し、既存データは変更しない
- OK なら確認ダイアログ（「現在のタスクは置き換えられます。続けますか？」）を出してから上書き

## 4. UI 構造

### 全体レイアウト

```
┌────────────────────────────────────────────────────────────┐
│  ヘッダー: タイトル「タスク」  [エクスポート] [インポート]   │
├────────────────────────────────────────────────────────────┤
│  入力欄: [タスクを追加...                        ] [追加]   │
├────────────────────────────────────────────────────────────┤
│  ▼ 期限切れ (3)                                             │
│    ☐ ★ 〇〇                                                │
│    ☐    △△                                                │
│  ▼ 今日 (2)                                                 │
│    ...                                                      │
│  ▼ 明日 (1)                                                 │
│  ▼ 今週 (4)                                                 │
│  ▼ それ以降 (2)                                             │
│  ▼ 期限なし (5)                                             │
│  ───                                                        │
│  ▼ 完了 (12)                                                │
│    ☑ ××（取り消し線）                                     │
└────────────────────────────────────────────────────────────┘
```

タスクをクリックすると右側からサイドパネルがスライドインする:

```
┌──────────────────┬────────────────────────┐
│  メインリスト    │  詳細パネル            │
│  （上記のまま）  │  タイトル: [_________]  │
│                  │  期限: [今日][明日][今週末][📅] │
│                  │  ☆ 重要                │
│                  │  メモ:                  │
│                  │  [_________________]   │
│                  │  ...                    │
│                  │  [削除] [閉じる]        │
└──────────────────┴────────────────────────┘
```

### 主要コンポーネント

- `App` — 全体のレイアウトと状態保持
- `Header` — タイトルとエクスポート/インポートボタン
- `TaskInput` — 上部のテキスト入力欄（Enter で追加）
- `TaskGroup` — 1 つの期限グループ（見出し + タスク一覧）
- `TaskItem` — 1 件のタスク行（チェックボックス、★アイコン、タイトル）
- `CompletedSection` — 完了タスクの折りたたみセクション
- `DetailPanel` — 右側サイドパネル（タスクの全項目編集）
- `ConfirmDialog` — 削除・インポート時の確認モーダル
- `Toast` — エラー通知用

### 空状態

タスクが 1 件もないとき: 中央に薄い文字で「タスクを追加してみましょう」と表示。

## 5. 期限グループ分けロジック

### グループの定義（today = 今日の日付）

| グループ名  | 条件 |
|------------|------|
| 期限切れ    | `dueDate` が today より前、かつ未完了 |
| 今日        | `dueDate === today` |
| 明日        | `dueDate === today + 1日` |
| 今週        | `dueDate` が「明日」より後、かつ「今週の日曜日」以前 |
| それ以降    | `dueDate` が「今週の日曜日」より後 |
| 期限なし    | `dueDate === null` |

完了タスクは上記のグループ分けに含めず、別の「完了」セクションへ。

### 「今週末」ボタンの動作

入力時の「今週末」ボタン: **今週の土曜日** の日付をセット（今日が土曜なら今日、日曜なら 6 日後の土曜）。

### 週の始まりと終わり

- 週の始まり: 月曜
- 週の終わり: 日曜
- ゆえに「今週」グループ = 今日が水曜なら、明日以降〜次の日曜まで

### 擬似コード

```ts
function groupOf(task: Task, today: Date): GroupName {
  if (task.dueDate === null) return "期限なし";
  const due = parseDate(task.dueDate);
  if (isBefore(due, today)) return "期限切れ";
  if (isSameDay(due, today)) return "今日";
  if (isSameDay(due, addDays(today, 1))) return "明日";
  const sundayThisWeek = endOfWeekSunday(today); // 月始まりの週の日曜
  if (isBeforeOrSame(due, sundayThisWeek)) return "今週";
  return "それ以降";
}
```

### グループ内のソート順

1. 重要なタスクが上
2. その中で作成日時（`createdAt`）の昇順

完了セクション内も同じソート規則を用いる（重要 → 作成日時順）。

## 6. 14 日自動クリアの仕様

### 目的

「期限切れ」が長期放置されると一覧が赤いタスクだらけになり視認性が落ちる。14 日以上前の期限を持つ未完了タスクは、もはや「期限」として機能していないとみなし、期限をクリアして「期限なし」グループに移す。

### 実行タイミング

- アプリ起動時に 1 回だけ実行
- 同日の再リロードでは再実行しない（`lastCleanupDate` を見て、今日と異なる日にのみ実行）

### 対象

- `completed === false`
- `dueDate !== null`
- `dueDate < today - 14日`

### 処理

該当タスクの `dueDate` を `null` に書き換え、`lastCleanupDate` を今日の日付（YYYY-MM-DD）で更新して localStorage に保存。

### 通知

クリア件数が 1 件以上なら、画面にトーストで「N 件の古い期限をクリアしました」と表示。

## 7. 操作仕様

### マウス操作

| 操作               | 対象         | 結果 |
|--------------------|--------------|------|
| クリック            | 入力欄        | フォーカス、テキスト入力可 |
| Enter              | 入力欄        | タスクを追加。フォーカスは入力欄に残る |
| クリック            | チェックボックス | 完了 ↔ 未完了をトグル（サイドパネルは開かない） |
| クリック            | ★アイコン     | 重要トグル（サイドパネルは開かない） |
| クリック            | タスク本体（タイトル部分） | サイドパネルが開き、全項目を編集 |
| クリック            | グループ見出し | グループの折りたたみ展開トグル |
| クリック            | サイドパネルの削除ボタン | 確認ダイアログを経由してから削除 |
| クリック            | サイドパネル外 | サイドパネルを閉じる（変更は自動保存済み） |

### キーボード操作

| キー         | 文脈            | 結果 |
|--------------|-----------------|------|
| Enter        | 入力欄でフォーカス時 | タスクを追加 |
| Tab / Shift+Tab | 任意 | ブラウザ標準のフォーカス移動 |
| ↑ / ↓        | タスクリスト上 | 上下のタスク行へフォーカス移動 |
| Space        | タスク行フォーカス時 | 完了 ↔ 未完了トグル |
| Delete       | タスク行フォーカス時 | 削除確認ダイアログを開く |
| Esc          | サイドパネル / ダイアログ表示中 | 閉じる |

### 期限入力（サイドパネル内）

- 4 つのボタン: `今日` / `明日` / `今週末` / `📅 カレンダー`
- カレンダーボタンは標準の `<input type="date">` を呼び出す
- 「クリア」ボタンで `dueDate = null` に戻せる

### 確認ダイアログの文言

- タスク削除: 「このタスクを削除します。よろしいですか？」 [キャンセル] [削除する]
- インポート: 「現在のタスクは置き換えられます。よろしいですか？」 [キャンセル] [置き換える]

## 8. テーマ追従の実装方針

- 手動切り替え UI は **作らない**
- `prefers-color-scheme` メディアクエリで OS 設定を検出
- 実装方法: CSS カスタムプロパティ（CSS 変数）で配色を定義し、`@media (prefers-color-scheme: dark)` で上書き

```css
:root {
  --bg: #ffffff;
  --fg: #1a1a1a;
  --muted: #999;
  --accent: #3b82f6; /* 実装中に確定 */
  --overdue: #ef4444;
  --border: #e5e7eb;
}

@media (prefers-color-scheme: dark) {
  :root {
    --bg: #0f0f10;
    --fg: #e5e5e5;
    --muted: #777;
    --accent: #60a5fa;
    --overdue: #f87171;
    --border: #27272a;
  }
}
```

- スタイル: Linear / Things に寄せた静かな見た目。装飾は最小限、余白広め、フォントはシステムフォント (`system-ui` 系)

## 9. デプロイ

### GitHub Pages + GitHub Actions

- GitHub リポジトリの `main` ブランチに push したら、Actions が自動で `npm run build` を実行
- `dist/` を `gh-pages` 環境にデプロイ
- 公開 URL は `https://<username>.github.io/<repo-name>/`

### Vite の base path

GitHub Pages はサブパス配信になるため、`vite.config.ts` で base を設定:

```ts
export default defineConfig({
  base: '/<repo-name>/',
  plugins: [react()],
});
```

### GitHub Actions ワークフロー（概略）

`.github/workflows/deploy.yml` の要点:

1. Checkout
2. Node.js セットアップ（LTS）
3. `npm ci`
4. `npm run typecheck && npm run lint && npm run build`
5. `actions/upload-pages-artifact@v3` で `dist/` をアップロード
6. `actions/deploy-pages@v4` で公開

リポジトリの Settings → Pages で「GitHub Actions」をソースに設定する。

## 10. 想定する規模感とパフォーマンス

- 同時保持件数: 数百件まで
- 仮想スクロール: 不要（数百件なら素朴な map レンダリングで体感問題なし）
- アニメーション: 控えめに（サイドパネルのスライドインと、トーストのフェードのみ）
- 起動時間の目標: ブックマークから開いて 1 秒以内に操作可能

## 11. 用語

- **期限**: タスクに紐づく日付（時刻なし）。期限切れの判定は「日」単位
- **重要**: 真偽値の 1 ビット。「優先度」とは呼ばない（段階がないため）
- **グループ**: 期限による自動分類のひと塊（期限切れ / 今日 / 明日 / 今週 / それ以降 / 期限なし）
- **セクション**: 「未完了の全グループ」と「完了」を分ける大きな区切り
